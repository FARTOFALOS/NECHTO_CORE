name: Docs

on:
  push:
    paths:
      - "docs/**"
      - "README.md"
  pull_request:
    paths:
      - "docs/**"
      - "README.md"

jobs:
  docs-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Docs workflow scaffold"
